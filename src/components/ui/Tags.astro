---
export interface Props {
	tags?: string[];
}

const { tags } = Astro.props;
---

{
	tags && (
		<div class="items-center justify-center">
			{tags.map((tag) => (
				<span class="inline-block rounded px-2 py-2 text-xs text-black lg:py-1">#{tag}</span>
			))}
		</div>
	)
}
